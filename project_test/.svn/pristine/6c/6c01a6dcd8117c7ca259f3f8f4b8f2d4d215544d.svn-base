local RecieveRunMsgProcess = class("RecieveRunMsgProcess")

function RecieveRunMsgProcess:ctor()
	--响应消息号
	self.order = 11007
	--用户id
	self.userid =  ""
	--uuid
	self.uuid =  ""
	--token值
	self.token =  ""	
end


function RecieveRunMsgProcess:serialization()
	local data = {
		order = self.order,
		userid = self.userid,
		uuid = self.uuid,
		token = self.token,
		
	}
	return data
end

return RecieveRunMsgProcess