local FlopModel = class("FlopModel")

function FlopModel:ctor(param)
	self.needMoney = 100
	self.freshCost = 100
	--翻牌次数
	self.times = 0
	--当前充值金额
	self.nowMoney = 0
	--获得物品数据
	self.getItems = {}
	--全部开启必获得物品数据
	self.hopeItems = {}
	--已开启位置
	self.poses = {}
end

function FlopModel:update(param)

end

function FlopModel:createItem(param)
	local item = {
		id = "",
		uId = "",
		count = "",
		type_ = "",
	}
	return item
end

return FlopModel